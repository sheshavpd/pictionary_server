const redisPub = require('../../credentials/redis_publisher');
/*const { promisify } = require("util");
const getAsync = promisify(redisPub.get).bind(redisPub);
const setAsync = promisify(redisPub.set).bind(redisPub);*/


const gameLoop = async function() {

};

setInterval(gameLoop, 1000);
